# Minimal project

From this directory after installing the runtime:

```bash
farm init
farm task-create --objective "demo" --deliverable "result.md" --acceptance "manual acceptance"
farm status
farm doctor
```
