"""farmboard: read-only rendering of a farm (Textual TUI + static HTML).

Actions are command generators executed through the pinned `farm` wrapper after
confirmation; farmboard never writes farm state (D19, D28-D30).
"""
__version__ = "0.1.0.dev0"
